#### v-for与v-if哪个优先级更高
#### v-for的优先级更高，因为v-if依赖v-for遍历出来的变量，需要再次进行计算，才能进行dom的更新。
// 如果必须要使用v-for和v-if的时候，可以采用以下两种方式
##### 1.如果同一个dom上面需要同时使用 v-for和v-if时，可以使用v-show进行判断，来提高性能。
##### 2.如果需要使用v-if v-else 的时候，也可以不在同一个dom上使用，可以在子节点上面使用。
